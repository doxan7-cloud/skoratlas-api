services:
  - type: web
    name: skoratlas-api
    runtime: node
    plan: free
    buildCommand: npm ci --omit=dev
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: API_FOOTBALL_KEY
        sync: false
      - key: ALLOWED_ORIGIN
        value: "*"

